web = {
    /***
     * Tải 1 chuỗi dữ liệu từ internet
     * @param url phải chứa http:// hoặc https://
     * @returns {Promise<void>} sử dụng .then(data=>{}) và .catch(error) để xử lý
     */
    get: async (url) => {},
    post: async (url, datapost) => {}

}
